language: c
compiler:
  - gcc

before_install:
  - sudo apt-add-repository -y ppa:vala-team/ppa
  - sudo apt-get update -qq > /dev/null
  - sudo apt-get install -qq valac libglib2.0-dev itstool debhelper pkg-config > /dev/null

sudo: required
dist: trusty

script:
  - make
  
after_success:
 - echo "Nice"

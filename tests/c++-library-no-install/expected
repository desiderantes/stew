!bake
mkdir -p .built
g++ -fPIC -MMD -MF .built/test.d -c test.cpp -o .built/test.o
g++ -o libtest.so.0 -shared .built/test.o
(exit SUCCESS)
!bake install
(exit SUCCESS)
!bake clean
rm -f libtest.so.0
rm -rf .built/
rm -f libtest.so
rm -f test-0.pc
(exit SUCCESS)

!bake
mkdir -p .built
g++ -MMD -MF .built/test.d -c test.cpp -o .built/test.o
g++ -o test .built/test.o
(exit SUCCESS)
!bake install
(exit SUCCESS)
!bake clean
rm -f test
rm -rf .built/
(exit SUCCESS)

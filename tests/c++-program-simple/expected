!bake
mkdir -p .built
g++ -MMD -MF .built/test.d -c test.cpp -o .built/test.o
g++ -o test .built/test.o
(exit SUCCESS)
!bake install
mkdir -p /usr/bin
cp test /usr/bin/test
(exit SUCCESS)
!bake clean
rm -f test
rm -rf .built/
(exit SUCCESS)

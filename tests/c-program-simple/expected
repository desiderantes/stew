!bake
mkdir -p .built
gcc -MMD -MF .built/test.d -c test.c -o .built/test.o
gcc -o test .built/test.o
(exit SUCCESS)
!bake install
mkdir -p /usr/bin
cp test /usr/bin/test
(exit SUCCESS)
!bake clean
rm -f test
rm -rf .built/
(exit SUCCESS)

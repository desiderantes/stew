!bake
gmcs -out:test.exe test.cs
mkdir -p .built
(exit SUCCESS)
!bake install
mkdir -p /usr/local/share/test
cp test.exe /usr/local/share/test/test.exe
mkdir -p /usr/local/bin
cp .built/test /usr/local/bin/test
(exit SUCCESS)
!bake clean
rm -f test.exe
rm -f test
rm -rf .built/
(exit SUCCESS)

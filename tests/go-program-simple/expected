!bake
mkdir -p .built
gccgo -c test.go -o .built/test.o
gccgo -o test .built/test.o
(exit SUCCESS)
!bake install
mkdir -p /usr/local/bin
cp test /usr/local/bin/test
(exit SUCCESS)
!bake clean
rm -f test
rm -rf .built/
(exit SUCCESS)

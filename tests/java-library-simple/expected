!bake
mkdir -p .built
javac -d .built Test.java
jar cfe test.jar -C .built Test.class
(exit SUCCESS)
!bake install
mkdir -p /usr/local/share/java
cp test.jar /usr/local/share/java/test.jar
(exit SUCCESS)
!bake clean
rm -f test.jar
rm -rf .built/
(exit SUCCESS)

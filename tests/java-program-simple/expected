!bake
mkdir -p .built
javac -d .built Test.java
jar cfe test.jar -C .built Test.class
(exit SUCCESS)
!bake install
mkdir -p /usr/local/share/test
cp test.jar /usr/local/share/test/test.jar
mkdir -p /usr/local/bin
cp .built/test /usr/local/bin/test
(exit SUCCESS)
!bake clean
rm -f test.jar
rm -rf .built/
rm -f test
(exit SUCCESS)

!bake
python -m py_compile test.py
mkdir -p .built
(exit SUCCESS)
!bake install
(exit SUCCESS)
!bake clean
rm -f test.pyc
# FIXME: Duplicate deletes
rm -f test.pyc
rm -f test
rm -f test.pyc
rm -rf .built/
(exit SUCCESS)
